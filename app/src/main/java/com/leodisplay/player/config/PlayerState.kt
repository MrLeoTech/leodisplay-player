package com.leodisplay.player.config

/**
 * Official states for the LeoDisplay Player.
 */
enum class PlayerState {
    ACTIVATING,
    ONLINE,
    OFFLINE,
    UPDATING,
    RESTARTING,
    MAINTENANCE,
    ERROR
}
